import { StyleSheet } from "react-native";

export default StyleSheet.create({
    lottie:{
        width: 200,
        height: 200,
        backgroundColor: '#eee',
    },
    animationContainer: {
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
    },
})